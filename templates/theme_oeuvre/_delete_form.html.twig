<form method="post" action="{{ path('app_theme_oeuvre_delete', {'id': theme_oeuvre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ theme_oeuvre.id) }}">
    <button class="btn">Delete</button>
</form>
