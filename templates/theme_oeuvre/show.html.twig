{% extends 'base.html.twig' %}

{% block title %}ThemeOeuvre{% endblock %}

{% block body %}
    <h1>ThemeOeuvre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ theme_oeuvre.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_theme_oeuvre_index') }}">back to list</a>

    <a href="{{ path('app_theme_oeuvre_edit', {'id': theme_oeuvre.id}) }}">edit</a>

    {{ include('theme_oeuvre/_delete_form.html.twig') }}
{% endblock %}
